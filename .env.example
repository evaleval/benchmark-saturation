# Copy this file to .env and fill in your own values.
# .env is gitignored — never commit real secrets.

# Semantic Scholar API key, used by analyzer/src/metrics/dynamic/citation_metric.py
# to fetch citation counts. Optional but rate-limited without one.
# Request a key at https://www.semanticscholar.org/product/api
SEMANTIC_SCHOLAR_API_KEY=
